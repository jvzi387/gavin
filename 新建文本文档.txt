项目1：Pandas 数据清洗实战
数据集：用户订单表（含缺失值、重复值、异常值、格式错误）

练习：

用 pandas 读取 CSV，输出基础信息（shape、dtypes、describe）

处理缺失值：金额列用中位数填充，地区列用众数填充

删除重复订单（按订单号）

过滤异常值：金额 ≤ 0 或 > 100000 的行标记并剔除

转换日期列为 datetime 类型，提取年、月、日、星期几

输出清洗后的数据概览和统计报告

项目2：分组聚合与透视表
数据集：电商销售记录（订单日期、产品类别、销售额、数量、用户ID）

练习：

按月统计总销售额和总销量

按产品类别统计：平均客单价、总订单数

找出每个类别下销售额 top 3 的用户

用 pivot_table 生成：行=月份，列=产品类别，值=销售额

用 groupby + agg 同时计算：总和、均值、中位数、计数

计算每个用户的复购率（购买次数 ≥ 2 的比例）

项目3：数据合并与连接
数据集：orders.csv、customers.csv、products.csv

练习：

用 merge 将订单与用户信息合并（按 user_id）

用 merge 将上一步结果与产品信息合并（按 product_id）

找出没有购买记录的用户（left join + 过滤 null）

用 concat 合并两个月的数据，并标记数据来源月份

处理合并后的重复列（如两个表的 id 字段）

统计每个用户的首次购买日期和最后一次购买日期

项目4：apply 与自定义函数
数据集：员工打卡记录（姓名、日期、上班时间、下班时间）

练习：

写函数计算工作时长（下班 - 上班，处理跨天情况）

用 apply 为每行添加工作时长列

写函数判断是否迟到（上班 > 9:00），用 apply 添加迟到标记

用 apply 将工作时长分类：<6小时、6-8小时、>8小时

用 apply 结合 lambda 提取日期中的星期几

用 applymap 将整个 DataFrame 中的空字符串替换为 None

项目5：数据可视化（matplotlib + seaborn）
数据集：天气记录（日期、最高温、最低温、降雨量、风速）

练习：

画折线图：日最高温变化趋势（用 matplotlib）

画柱状图：每月平均降雨量（用 seaborn）

画散点图：风速 vs 温差，添加趋势线

画箱线图：各月份最高温分布对比

画直方图：降雨量分布，设置 bins=30

组合图：双 Y 轴（温度 + 降雨量）

项目6：时间序列处理
数据集：股票日线数据（日期、开盘、收盘、最高、最低、成交量）

练习：

将日期设为索引，按周重采样计算每周均价

计算 5 日、20 日移动平均线

计算每日收益率（收盘价变化百分比）

找出连续上涨（涨跌幅 > 0）的最长天数

按月分组统计：月收益率、月均成交量

用 shift 计算当日与昨日收盘价差值

项目7：字符串与文本处理
数据集：用户评论表（评论ID、用户昵称、评论文本、评分）

练习：

用 str 方法提取昵称中的中文字符

统计评论文本长度分布

找出包含特定词（如“好”“差”“退”）的评论

用正则提取评论中的手机号、金额数字

将评分 1-2 星标记为“差评”，4-5 星为“好评”

统计每个用户的平均评论长度和评论数

项目8：NumPy 向量化运算
数据集：生成模拟数据（10000 行，5 个特征）

练习：

用 numpy 生成随机数组：正态分布、均匀分布各一个

计算数组的均值、标准差、中位数（不用 pandas）

向量化实现：将数组中的负数替换为 0

矩阵乘法：生成两个 100x100 随机矩阵并相乘

广播运算：对每一列减去该列的均值

用 where 实现条件赋值：大于阈值设为 1，否则 0

项目9：性能优化与向量化
数据集：大订单表（10 万行以上）

练习：

用 time 模块对比：for 循环 vs apply vs 向量化（计算折扣后价格）

用 pandas 的 eval 或 query 加速复杂条件筛选

将 object 类型的分类列转为 category 类型，对比内存占用

用分块读取处理超过内存的大文件（chunksize）

用 numpy 的 where 替代多重 if-else

记录每种方法的耗时，写出优化结论

项目10：完整数据分析流水线
数据集：银行客户流失数据（10+ 字段，含数值和分类变量）

练习：

完成完整 EDA：缺失值、异常值、分布、相关性

特征工程：年龄分段、收入分层、使用时长分组

用 groupby 分析不同群体流失率的差异

创建流失风险评分规则（自定义权重计算）

输出一份分析报告（含代码、图表、结论）

将整个流程封装成函数，输入文件路径输出报告