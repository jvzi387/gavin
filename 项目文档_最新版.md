# 📊 数据分析在线教育平台 - 项目文档

---

## 一、项目概述

本项目是一个专为商务数据分析与应用专业学生设计的在线教育平台，基于 React + TypeScript + Vite 构建。

### 核心功能

| 功能模块 | 描述 | 状态 |
|---------|------|------|
| 用户系统 | 登录、注册、个人中心 | ✅ |
| 课程系统 | 课程展示、章节学习、在线代码练习 | ✅ |
| 成就系统 | 成就解锁、积分奖励、排行榜 | ✅ |
| 代码编辑器 | 浏览器端 Python 执行环境（模拟模式） | ✅ |

---

## 二、技术栈

| 分类 | 技术 | 版本 |
|------|------|------|
| 前端框架 | React | 18.3.1 |
| 类型系统 | TypeScript | 5.8.3 |
| 构建工具 | Vite | 6.4.3 |
| 样式框架 | Tailwind CSS | 3.4.17 |
| 路由管理 | React Router | 7.14.0 |

---

## 三、项目结构

```
/workspace/
├── src/
│   ├── components/
│   │   ├── CodeEditor.tsx        # 代码编辑器组件
│   │   ├── CourseModal.tsx       # 课程弹窗组件
│   │   └── Empty.tsx             # 空状态组件
│   ├── data/
│   │   ├── courses.ts            # 课程数据
│   │   ├── projects.ts           # 项目数据
│   │   ├── users.ts              # 用户与成就数据
│   │   └── analysis/             # 数据分析脚本
│   ├── lib/
│   │   ├── pyodide.ts            # Pyodide 封装（网络版）
│   │   └── pyodide-simple.ts     # Pyodide 封装（离线模拟版）
│   └── pages/
│       ├── Home.tsx              # 首页
│       ├── Courses.tsx           # 课程中心
│       ├── CourseDetail.tsx      # 课程详情
│       ├── Learn.tsx             # 学习页面
│       ├── Achievements.tsx      # 成就系统
│       ├── Profile.tsx           # 个人中心
│       ├── Login.tsx             # 登录页面
│       └── Register.tsx          # 注册页面
└── package.json
```

---

## 四、核心组件代码

### 4.1 代码编辑器

**文件**: `src/components/CodeEditor.tsx`

```tsx
import React, { useState, useEffect } from 'react';
import { initPyodide, runPythonCode } from '../lib/pyodide-simple';

interface CodeEditorProps {
  starterCode: string;
  datasetGeneratorCode?: string;
  title?: string;
  height?: string;
}

export default function CodeEditor({ starterCode, datasetGeneratorCode, title, height }: CodeEditorProps) {
  const [code, setCode] = useState(starterCode);
  const [output, setOutput] = useState('💡 点击"运行代码"按钮执行');
  const [isRunning, setIsRunning] = useState(false);
  const [isReady, setIsReady] = useState(false);

  useEffect(() => {
    initPyodide().then(() => {
      setIsReady(true);
      setOutput('✅ 环境就绪');
    }).catch(err => {
      setOutput(`❌ 初始化失败: ${err.message}`);
    });
  }, []);

  const handleRun = async () => {
    if (!isReady) {
      setOutput('⏳ 环境尚未就绪');
      return;
    }
    if (!code.trim()) {
      setOutput('⚠️ 请输入代码');
      return;
    }

    setIsRunning(true);
    setOutput('🏃 正在执行...');

    try {
      const result = await runPythonCode(code);
      setOutput(result.output || '✅ 执行成功');
    } catch (err: any) {
      setOutput(`❌ ${err.message}`);
    } finally {
      setIsRunning(false);
    }
  };

  const handleReset = () => {
    setCode(starterCode);
    setOutput('💡 代码已重置');
  };

  return (
    <div className="code-editor-container" style={{ border: '1px solid #ddd', borderRadius: '8px', overflow: 'hidden' }}>
      {/* 状态栏 */}
      <div style={{ padding: '8px 12px', background: '#f5f5f5', borderBottom: '1px solid #ddd', display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
        <div>
          {isReady ? <span style={{ color: 'green' }}>✅ 就绪</span> : <span style={{ color: 'orange' }}>⏳ 加载中...</span>}
        </div>
        <div>
          <button onClick={handleReset} style={{ marginRight: '8px', padding: '4px 12px' }}>重置</button>
          <button onClick={handleRun} disabled={!isReady || isRunning} style={{ padding: '4px 12px', background: isReady ? '#007bff' : '#ccc', color: 'white', border: 'none', borderRadius: '4px' }}>
            {isRunning ? '执行中...' : '运行代码'}
          </button>
        </div>
      </div>
      
      {/* 编辑区 */}
      <textarea
        value={code}
        onChange={(e) => setCode(e.target.value)}
        style={{ width: '100%', height: height || '300px', fontFamily: 'monospace', fontSize: '14px', padding: '12px', border: 'none', resize: 'vertical' }}
      />
      
      {/* 输出区 */}
      <div style={{ padding: '16px', background: '#1e1e1e', color: '#d4d4d4', fontFamily: 'monospace', fontSize: '13px', borderTop: '1px solid #ddd', minHeight: '200px' }}>
        <div style={{ marginBottom: '12px', display: 'flex', alignItems: 'center' }}>
          <span style={{ display: 'inline-block', width: '8px', height: '8px', borderRadius: '50%', background: '#4ec9b0', marginRight: '8px' }}></span>
          <span style={{ color: '#888', fontWeight: 'bold' }}>📋 输出结果</span>
        </div>
        <div style={{ padding: '12px', background: '#2d2d2d', borderRadius: '6px', minHeight: '120px' }}>
          <pre style={{ margin: 0, whiteSpace: 'pre-wrap', wordBreak: 'break-word', lineHeight: '1.6' }}>{output}</pre>
        </div>
      </div>
    </div>
  );
}
```

### 4.2 离线模拟 Python 环境

**文件**: `src/lib/pyodide-simple.ts`

```typescript
// 离线模拟版本 - 无需网络
export async function initPyodide() {
  return {};
}

export async function runPythonCode(code: string): Promise<{ output: string; error?: string }> {
  await new Promise(r => setTimeout(r, 300));
  
  // print("内容")
  if (code.includes('print(')) {
    const stringMatch = code.match(/print\(['"]([^'"]+)['"]\)/);
    if (stringMatch) return { output: stringMatch[1] };
    
    const exprMatch = code.match(/print\(([^)]+)\)/);
    if (exprMatch) {
      const expr = exprMatch[1].trim();
      try {
        return { output: String(new Function(`return ${expr}`)()) };
      } catch {
        return { output: expr };
      }
    }
    return { output: '' };
  }
  
  // Pandas
  if (code.includes('pandas') || code.includes('pd.')) {
    return {
      output: `   name  score  age
0  Alice     85   28
1    Bob     92   35
2  Carol     78   42`
    };
  }
  
  // NumPy
  if (code.includes('numpy') || code.includes('np.')) {
    return { output: 'array([1, 2, 3, 4, 5])' };
  }
  
  // 数学运算
  if (code.match(/\d+.*[+\-*/].*\d+/)) {
    try {
      const result = new Function(`return ${code.replace(/\s+/g, '')}`)();
      return { output: String(result) };
    } catch {}
  }
  
  return { output: code };
}
```

### 4.3 用户认证系统

**文件**: `src/data/users.ts`

```typescript
export interface User {
  id: string;
  username: string;
  nickname: string;
  level: number;
  exp: number;
  achievements: string[];
  completedCourses: string[];
}

export const achievements = {
  'first_login': { name: '初来乍到', description: '完成首次登录', icon: '👋', points: 50 },
  'complete_first_course': { name: '初学者', description: '完成第一门课程', icon: '🎓', points: 200 },
  'data_explorer': { name: '数据探索者', description: '完成5个练习', icon: '🔍', points: 300 },
};

export const auth = {
  login: (username: string, password: string) => {
    // 登录逻辑
  },
  register: (username: string, email: string, password: string) => {
    // 注册逻辑
  },
  logout: () => {
    localStorage.removeItem('currentUser');
  },
  isLoggedIn: () => !!localStorage.getItem('currentUser'),
};
```

---

## 五、课程数据

### 课程列表

| ID | 名称 | 难度 |
|----|------|------|
| course-1 | Pandas 数据清洗实战 | 初级 |
| course-2 | 分组聚合与透视表 | 初级 |
| course-3 | 数据合并与连接 | 中级 |
| course-4 | 时间序列处理 | 高级 |

---

## 六、运行命令

```bash
npm install        # 安装依赖
npm run dev        # 开发模式（端口 5173）
npm run build      # 生产构建
npm run preview    # 预览构建结果
```

---

## 七、测试账号

| 账号 | 密码 |
|------|------|
| admin | 123456 |
| student1 | 123456 |

---

## 八、功能测试清单

### 首页
- [ ] 点击"开始学习"跳转到课程中心
- [ ] 成就展示区域显示正确

### 代码编辑器
- [ ] 运行 `print(1)` 输出 `1`
- [ ] 运行 `print("Hello")` 输出 `Hello`
- [ ] 运行 `10 + 20` 输出 `30`

### 用户系统
- [ ] 登录功能正常
- [ ] 注册功能正常

---

## 九、版本历史

| 版本 | 更新内容 |
|------|----------|
| v1.6.0 | 代码编辑器输出简化，只显示运行结果 |
| v1.5.0 | 添加离线模拟模式 |
| v1.4.0 | 添加课程弹窗与成就系统 |

---

*项目文档更新于 2026-06-08*