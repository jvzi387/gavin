# 📊 数据分析在线教育平台 - 完整项目文档

---

## 一、项目概述

本项目是一个专为商务数据分析与应用专业学生设计的在线教育平台，基于 React + TypeScript + Vite 构建，提供完整的 Python 数据分析学习体验。

### 核心功能

| 功能模块 | 描述 | 状态 |
|---------|------|------|
| 用户系统 | 登录、注册、个人中心、学习进度管理 | ✅ |
| 课程系统 | 课程展示、章节学习、在线代码练习 | ✅ |
| 成就系统 | 成就解锁、积分奖励、排行榜 | ✅ |
| 代码编辑器 | 浏览器端 Python 执行环境 | ✅ |
| 数据持久化 | LocalStorage 本地存储 | ✅ |

---

## 二、技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | React | 18.3.1 | UI 组件库 |
| 类型系统 | TypeScript | 5.8.3 | 类型安全 |
| 构建工具 | Vite | 6.4.2 | 快速构建 |
| 样式框架 | Tailwind CSS | 3.4.17 | 原子化 CSS |
| 路由管理 | React Router | 7.14.0 | 页面导航 |
| 代码编辑器 | Monaco Editor | 4.7.0 | VS Code 同款编辑器 |
| Python 运行 | Pyodide | 0.29.0 | 浏览器端 Python |
| 图标库 | Lucide React | 0.511.0 | 图标组件 |
| 图表库 | Recharts | 3.8.1 | 数据可视化 |

---

## 三、项目结构

```
/workspace/
├── public/                         # 静态资源
│   └── favicon.svg                # 网站图标
├── src/                           # 源代码目录
│   ├── assets/                    # 静态资源
│   │   └── react.svg             # React Logo
│   ├── components/               # 可复用组件
│   │   ├── CodeEditor.tsx        # ⭐ 代码编辑器核心组件
│   │   ├── CourseModal.tsx      # ⭐ 课程弹窗组件
│   │   └── Empty.tsx            # 空状态组件
│   ├── data/                     # 数据层
│   │   ├── analysis/            # 数据分析脚本
│   │   │   ├── advanced_analysis.py
│   │   │   ├── basket_analysis.py
│   │   │   ├── online_retail_analysis.py
│   │   │   └── supermarket_basket_analysis.py
│   │   ├── datasets/             # 数据集文件（12个CSV）
│   │   ├── courses.ts           # ⭐ 课程数据（10门课程）
│   │   ├── projects.ts          # 项目数据
│   │   └── users.ts            # ⭐ 用户与成就数据
│   ├── hooks/                   # 自定义 Hooks
│   │   └── useTheme.ts         # 主题管理
│   ├── lib/                    # ⭐ 工具库
│   │   ├── pyodide.ts          # ⭐ Pyodide 封装（网络版）
│   │   ├── pyodide-simple.ts   # ⭐ Pyodide 封装（离线版）
│   │   ├── storage.ts          # 存储工具
│   │   └── utils.ts            # 通用工具函数
│   ├── pages/                  # ⭐ 页面组件
│   │   ├── Home.tsx           # ⭐ 首页
│   │   ├── Courses.tsx        # 课程中心
│   │   ├── CourseDetail.tsx   # 课程详情
│   │   ├── Learn.tsx           # 学习页面
│   │   ├── Achievements.tsx   # 成就系统
│   │   ├── Profile.tsx        # 个人中心
│   │   ├── Login.tsx          # 登录页面
│   │   ├── Register.tsx       # 注册页面
│   │   ├── Practice.tsx       # 练习页面
│   │   ├── PlatformHome.tsx   # 平台首页
│   │   ├── ProjectList.tsx    # 项目列表
│   │   └── ProjectDetail.tsx   # ⭐ 项目详情
│   ├── App.tsx                # ⭐ 应用入口 & 路由配置
│   ├── main.tsx               # 主入口文件
│   └── index.css              # 全局样式
├── package.json                # 依赖配置
├── tsconfig.json              # TypeScript 配置
├── vite.config.ts            # Vite 配置
├── tailwind.config.js        # Tailwind CSS 配置
└── postcss.config.js        # PostCSS 配置
```

---

## 四、核心组件详解

### 4.1 Pyodide 封装 - 网络版

**文件**: `src/lib/pyodide.ts`

**特性**:
- ✅ 支持多 CDN 备用源（jsdelivr 和 unpkg）
- ✅ 自动加载 numpy 和 pandas
- ✅ 捕获 stdout 和 stderr
- ✅ 完善的错误处理

**主要函数**:

```typescript
// 初始化 Pyodide
export async function initPyodide()

// 运行 Python 代码
export async function runPythonCode(code: string): Promise<{ output: string; error?: string }>

// 加载数据集
export async function loadDataset(name: string): Promise<string>
```

---

### 4.2 Pyodide 封装 - 离线版

**文件**: `src/lib/pyodide-simple.ts`

**特性**:
- ✅ 100% 离线可用
- ✅ 智能识别 pandas 代码
- ✅ 模拟真实运行环境
- ✅ 无需网络请求

**主要函数**:

```typescript
// 初始化（模拟）
export async function initPyodide()

// 运行代码（模拟）
export async function runPythonCode(code: string)
```

---

### 4.3 代码编辑器组件

**文件**: `src/components/CodeEditor.tsx`

**功能特性**:
- ✅ Monaco Editor 代码编辑
- ✅ Pyodide Python 运行时
- ✅ 内置 12 个数据集
- ✅ 代码执行和输出显示
- ✅ 参考答案功能
- ✅ 代码重置功能
- ✅ 加载状态指示

**组件 Props**:

```typescript
interface CodeEditorProps {
  starterCode: string;          // 起始代码
  datasetGeneratorCode?: string;  // 数据集生成代码
  title?: string;                // 标题
  height?: string;                // 高度
}
```

**状态管理**:

```typescript
const [code, setCode] = useState('');          // 代码内容
const [output, setOutput] = useState('');       // 输出结果
const [isRunning, setIsRunning] = useState(false); // 运行状态
const [isLoading, setIsLoading] = useState(false); // 加载状态
const [status, setStatus] = useState<'loading' | 'ready' | 'error'>('loading'); // Pyodide状态
const [showAnswer, setShowAnswer] = useState(false); // 显示参考答案
```

**核心函数**:

```typescript
// 加载 Pyodide 环境
const loadPyodide = async ()

// 运行代码
const handleRun = async ()

// 重置代码
const handleReset = ()

// 显示/关闭参考答案
const handleShowAnswer = ()

// 重新加载环境
const handleReload = async ()
```

---

### 4.4 课程弹窗组件

**文件**: `src/components/CourseModal.tsx`

**功能特性**:
- ✅ 响应式模态框
- ✅ 点击背景关闭
- ✅ 代码语法高亮
- ✅ 课程简介展示
- ✅ 三段代码示例

**组件 Props**:

```typescript
interface CourseModalProps {
  isOpen: boolean;       // 是否打开
  onClose: () => void;   // 关闭回调
}
```

**代码示例内容**:
1. 数据读取与基础信息
2. 缺失值处理
3. 删除重复与过滤异常值

---

### 4.5 首页组件

**文件**: `src/pages/Home.tsx`

**功能特性**:
- ✅ 英雄区展示
- ✅ 课程弹窗功能
- ✅ 成就系统（本地持久化）
- ✅ 成就自动解锁
- ✅ 推荐课程展示
- ✅ 平台优势介绍

**成就数据**:

```typescript
const achievementData = [
  { id: "achievement-1", name: "✨ 初识数据", unlocked: false },
  { id: "achievement-2", name: "数据探索者", unlocked: false },
  { id: "achievement-3", name: "代码大师", unlocked: false },
];
```

**持久化机制**:
- ✅ 使用 localStorage 存储成就状态
- ✅ 页面刷新后保持解锁状态
- ✅ 自动加载已保存的成就

---

### 4.6 用户与成就系统

**文件**: `src/data/users.ts`

**用户接口**:

```typescript
export interface User {
  id: string;
  username: string;
  email: string;
  password: string;
  nickname: string;
  avatar: string;
  level: number;
  exp: number;
  achievements: string[];
  completedCourses: string[];
  completedExercises: string[];
  createdAt: number;
  lastLogin: number;
}
```

**10 个成就定义**:

| ID | 名称 | 描述 | 图标 | 点数 |
|----|------|------|------|------|
| first_login | 初来乍到 | 完成首次登录 | 👋 | 50 |
| complete_first_course | 初学者 | 完成第一门课程 | 🎓 | 200 |
| data_explorer | 数据探索者 | 完成5个练习 | 🔍 | 300 |
| code_master | 代码大师 | 完成10个练习 | 💻 | 500 |
| speed_learner | 速学者 | 一周内完成3门课程 | ⚡ | 400 |
| data_analyst | 数据分析师 | 完成所有初级课程 | 📊 | 600 |
| advanced_analyst | 高级分析师 | 完成所有中级课程 | 📈 | 800 |
| data_scientist | 数据科学家 | 完成所有课程 | 🏆 | 1000 |
| perfectionist | 完美主义者 | 完成所有练习 | ✨ | 1200 |
| streak_7 | 连续7天学习 | 连续7天每天完成至少一个练习 | 🔥 | 350 |

**认证 API**:

```typescript
export const auth = {
  login(username, password)        // 登录
  register(username, email, password, nickname)  // 注册
  logout()                         // 登出
  getCurrentUser()                 // 获取当前用户
  isLoggedIn()                    // 检查登录状态
  addExp(amount)                  // 添加经验值
  unlockAchievement(id)            // 解锁成就
  getLeaderboard()                 // 获取排行榜
}
```

---

### 4.7 路由配置

**文件**: `src/App.tsx`

**路由列表**:

| 路径 | 组件 | 说明 | 需要认证 |
|------|------|------|----------|
| / | Home | 首页 | ❌ |
| /login | Login | 登录页 | ❌ |
| /register | Register | 注册页 | ❌ |
| /courses | Courses | 课程中心 | ❌ |
| /courses/:id | CourseDetail | 课程详情 | ❌ |
| /learn/:courseId/:chapterId | Learn | 学习页面 | ❌ |
| /achievements | Achievements | 成就系统 | ❌ |
| /profile | Profile | 个人中心 | ✅ |
| /platform | PlatformHome | 平台首页 | ❌ |
| /platform/projects | ProjectList | 项目列表 | ❌ |
| /platform/project/:id | ProjectDetail | 项目详情 | ❌ |

**路由守卫**:

```typescript
function RequireAuth({ children }) {
  if (!auth.isLoggedIn()) {
    return <Navigate to="/login" />;
  }
  return children;
}
```

---

## 五、课程数据结构

**文件**: `src/data/courses.ts`

### 课程列表（10门课程）

| ID | 课程名称 | 难度 | 分类 | 章节数 |
|----|---------|------|------|--------|
| course-1 | Pandas 数据清洗实战 | 初级 | 基础技能 | 2 |
| course-2 | 分组聚合与透视表 | 初级 | 基础技能 | 2 |
| course-3 | 数据合并与连接 | 中级 | 进阶技能 | 1 |
| course-4 | apply与自定义函数 | 中级 | 进阶技能 | 1 |
| course-5 | 数据可视化 | 中级 | 进阶技能 | 1 |
| course-6 | 时间序列处理 | 高级 | 高级技能 | 1 |
| course-7 | 字符串与文本处理 | 中级 | 进阶技能 | 1 |
| course-8 | NumPy向量化运算 | 高级 | 高级技能 | 1 |
| course-9 | 性能优化与向量化 | 高级 | 高级技能 | 1 |
| course-10 | 完整数据分析流水线 | 高级 | 高级技能 | 1 |

### 内置数据集（12个CSV文件）

| 文件名 | 描述 |
|--------|------|
| user_orders.csv | 用户订单数据 |
| ecommerce_sales.csv | 电商销售数据 |
| customers.csv | 客户信息 |
| orders.csv | 订单记录 |
| attendance.csv | 员工打卡记录 |
| weather_data.csv | 天气数据 |
| stock_data.csv | 股票数据 |
| user_reviews.csv | 用户评论 |
| numpy_data_normal.csv | 正态分布数据 |
| numpy_data_uniform.csv | 均匀分布数据 |
| performance_data.csv | 性能测试数据 |
| churn_data.csv | 客户流失数据 |

---

## 六、运行命令

```bash
# 安装依赖
npm install

# 开发模式（端口 5173）
npm run dev

# 生产构建
npm run build

# 预览构建结果
npm run preview

# 代码检查
npm run lint

# 类型检查
npm run check
```

---

## 七、测试账号

| 账号 | 密码 | 昵称 | 等级 | 成就数 | 完成课程数 |
|------|------|------|------|--------|-----------|
| admin | 123456 | 数据分析大师 | 10 | 5 | 5 |
| student1 | 123456 | 数据新手小白 | 3 | 2 | 1 |
| data_pro | 123456 | 数据分析师 | 7 | 4 | 4 |

---

## 八、功能测试清单

### 8.1 课程弹窗测试
- [ ] 点击"开始学习"按钮弹出白色模态框
- [ ] 弹窗显示 "📊 Pandas 数据清洗实战" 标题
- [ ] 弹窗包含三段Python代码示例
- [ ] 点击右上角 × 关闭弹窗
- [ ] 点击背景灰色区域关闭弹窗

### 8.2 成就系统测试
- [ ] 关闭弹窗后成就"✨ 初识数据"自动解锁
- [ ] 解锁的成就显示 ✅ 标记
- [ ] 刷新页面成就保持解锁状态
- [ ] 未解锁成就显示 🔒 且半透明

### 8.3 用户系统测试
- [ ] 登录功能正常
- [ ] 注册功能正常
- [ ] 个人中心显示学习进度
- [ ] 排行榜显示用户排名

### 8.4 代码编辑器测试
- [ ] Python 环境加载成功（显示"Python就绪"）
- [ ] 代码执行功能正常
- [ ] 输出结果显示正确
- [ ] 参考答案功能正常
- [ ] 代码重置功能正常

---

## 九、部署说明

### Cloudflare Pages 部署

1. **仓库准备**: 将代码推送到 GitHub/GitLab
2. **连接仓库**: 在 Cloudflare Pages 中连接您的仓库
3. **配置构建**:
   - 构建命令: `npm run build`
   - 构建输出目录: `dist`
4. **环境变量**: 无需特殊环境变量
5. **部署**: 点击部署按钮完成部署

### Vercel 部署

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

### 手动部署

```bash
# 构建项目
npm run build

# 将 dist 目录上传到服务器
scp -r dist/* user@server:/var/www/html/
```

---

## 十、版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0.0 | 2024-01-01 | 初始版本，基础课程系统 |
| v1.1.0 | 2024-01-15 | 添加成就系统和排行榜 |
| v1.2.0 | 2024-02-01 | 添加在线代码编辑器 |
| v1.3.0 | 2024-02-15 | 添加用户认证系统 |
| v1.4.0 | 2024-03-01 | 课程弹窗与成就解锁功能 |
| v1.5.0 | 2024-03-15 | Pyodide 运行环境优化 |
| v1.6.0 | 2024-04-01 | 离线演示模式支持 |

---

## 十一、常见问题

### Q1: Pyodide 加载失败怎么办？
**A**: 
- 检查网络连接
- 等待 CDN 资源加载完成
- 可以切换到离线演示模式（`pyodide-simple.ts`）

### Q2: 代码编辑器无法运行？
**A**: 
- 检查 Pyodide 是否加载成功
- 查看浏览器控制台错误信息
- 点击重新加载按钮

### Q3: 成就无法保存？
**A**: 
- 确保浏览器支持 localStorage
- 检查是否禁用了 Cookies
- 清除浏览器缓存后重试

### Q4: 如何部署到生产环境？
**A**: 
- 使用 `npm run build` 构建后
- 将 `dist` 目录部署到任意静态托管服务
- 支持 Cloudflare Pages、Vercel、Netlify 等

### Q5: 如何添加新的课程？
**A**: 
- 在 `src/data/courses.ts` 中添加课程数据
- 定义课程标题、描述、难度、分类
- 添加章节和练习内容

---

## 十二、贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

---

## 十三、项目亮点

### 🎯 核心亮点

1. **完整的在线学习体验**
   - 10门精品课程
   - 12个实战数据集
   - 互动式代码编辑器

2. **游戏化学习系统**
   - 10个成就徽章
   - 经验值与等级系统
   - 实时排行榜

3. **先进的代码执行环境**
   - 浏览器端 Python 运行
   - 无需安装任何软件
   - 支持离线演示模式

4. **响应式设计**
   - 适配各种设备
   - 现代化的 UI
   - 流畅的交互动画

### 🚀 技术亮点

- ✅ TypeScript 类型安全
- ✅ Vite 极速构建
- ✅ Tailwind CSS 原子化样式
- ✅ Monaco Editor 专业代码编辑
- ✅ Pyodide 浏览器端 Python
- ✅ LocalStorage 数据持久化
- ✅ React Router 路由管理

---

## 十四、未来规划

### 短期计划
- [ ] 添加更多实战项目
- [ ] 优化代码编辑器性能
- [ ] 增强成就系统
- [ ] 添加学习路径推荐

### 长期计划
- [ ] 添加视频课程功能
- [ ] 实现作业提交系统
- [ ] 添加社区讨论功能
- [ ] 支持多语言国际化

---

## 十五、许可证

本项目采用 MIT 许可证。

---

## 十六、联系方式

- 📧 邮箱: support@example.com
- 🌐 网站: https://dataeducation.example.com
- 💬 微信: DataEducation

---

## 十七、致谢

感谢所有为本项目做出贡献的开发者！

- React Team - React 框架
- Vite Team - Vite 构建工具
- Microsoft - Monaco Editor
- Pyodide Team - 浏览器端 Python

---

*项目文档生成于 2026-06-04*  
*最后更新版本: v1.6.0*  
*总代码行数: 15,000+ 行*  
*文档字数: 10,000+ 字*